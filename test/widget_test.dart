import 'package:flutter_test/flutter_test.dart';

// Widget integration tests require Firebase to be initialized.
// See test/study_match_service_test.dart and test/user_model_test.dart
// for unit tests that run without a device.

void main() {
  testWidgets('placeholder — Firebase widget tests run on device',
      (tester) async {
    expect(true, isTrue);
  });
}
